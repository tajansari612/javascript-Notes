"use strict"; //Treat all js code as newer version

//alert(3+3); //This cause error as we are using node, not a brwoser

console.log(3 + 3);

// console.log(3
//     + 
//     3)  // Don't write like that readabilty should be high as possible

let name = "Taj";        //String
let age = 23;            //Number      
let isLoggedIn = false;  //Boolean
let state;               //Undefined

//bigint is use to represent big numbers
//null is a standalone value to represent empty
//symbol => unique

console.log(typeof(age));
console.log(typeof(state));
console.log(typeof(null));
